# Firebase Studio

This is a NextJS starter in Firebase Studio.

To get started, take a look at src/app/page.tsx.

## Environment variables

Create `web/.env.local` and paste the contents from `web/docs/env.template`, then replace placeholders with your real values.
